class Constants {
  // En escritorio/web conectamos directamente a localhost
  static const String apiUrl = 'http://localhost:3000';

  // Claves de persistencia
  static const String tokenKey = 'admin_token';
  static const String userKey = 'admin_data';
}
